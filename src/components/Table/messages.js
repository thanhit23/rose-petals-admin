import { defineMessages } from 'react-intl';

export const scope = 'Components.Table';

export default defineMessages({
  message_table: {
    id: `${scope}.message.table`,
    defaultMessage: "There aren't any lines.",
  },
});
